# ──────────────────────────────────────────────
# QShield Environment Variables
# Copy to .env and fill in values
# ──────────────────────────────────────────────

# ── Gateway Server ────────────────────────────
# Required for qshield-gateway deployment

# JWT secret for access/refresh tokens (min 32 chars)
QSHIELD_JWT_SECRET=change-me-to-a-random-64-char-string

# HMAC key for server-side evidence verification (min 32 chars)
QSHIELD_SERVER_HMAC_KEY=change-me-to-a-different-random-string

# Server port (default: 3847)
PORT=3847

# Server host (default: 0.0.0.0 for Docker/Railway)
HOST=0.0.0.0

# Node environment
NODE_ENV=production

# ── Optional ──────────────────────────────────

# Sentry DSN for crash reporting (optional)
# SENTRY_DSN=https://xxx@sentry.io/xxx

# Log level: debug, info, warn, error (default: info)
# LOG_LEVEL=info

# ── Desktop App ───────────────────────────────
# Desktop uses OS keychain (safeStorage) for secrets.
# No .env needed for the desktop app.
# Gateway URL is configured in Settings > Gateway.
