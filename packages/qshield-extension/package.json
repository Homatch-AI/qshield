{
  "name": "@qshield/extension",
  "version": "1.0.0",
  "description": "QShield Email Verification — Chrome/Edge Extension",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
