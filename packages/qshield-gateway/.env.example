# QShield Gateway — Environment Variables
# Copy to .env for local development

# Server
PORT=3847
HOST=0.0.0.0
NODE_ENV=development

# JWT secret for access/refresh tokens (min 32 chars)
QSHIELD_JWT_SECRET=dev-jwt-secret-min-32-chars-change-in-prod

# HMAC key for server-side evidence verification (min 32 chars)
QSHIELD_SERVER_HMAC_KEY=dev-hmac-key-min-32-chars-change-in-prod

# Database
DATABASE_PATH=./data/qshield-gateway.db

# Rate limiting
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=60000

# Log level: debug, info, warn, error
# LOG_LEVEL=debug
